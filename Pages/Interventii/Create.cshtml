@page "/Interventii/Create"

@model Proiect_aplicatieWeb.Pages.Interventii.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Interventie</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Interventie.Denumire" class="control-label"></label>
                <input asp-for="Interventie.Denumire" class="form-control" />
                <span asp-validation-for="Interventie.Denumire" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label" for="Interventie_SpecializareId">Specializare</label>
                <select class="form-control" asp-for="Interventie.SpecializareId" asp-items="@(ViewData["SpecializareId"] as SelectList)"></select>
            </div>
            <div class="form-group">
                <label asp-for="Interventie.Pret" class="control-label"></label>
                <input asp-for="Interventie.Pret" class="form-control" />
                <span asp-validation-for="Interventie.Pret" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
